{
  "name": "express",
  "version": "1.0.0",
  "description": "An Editor",
  "main": "index.js",
  "scripts": {
    "build_cmake": "cmake-js build -O build/Cmake",
    "build_GUI": "cd src/GUI && npm run build",
    "dev_GUI": "cd src/GUI && npm run dev",
    "build": "npm run build_cmake",
    "dev": "npm run build && npm run dev_GUI"
  },
  "author": "Soupup",
  "license": "",
  "devDependencies": {
    "cmake-js": "^7.3.0"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^8.1.0"
  },
  "cmake-js": {
    "runtime": "node",
    "runtimeVersion": "12.16.3",
    "arch": "x64"
  }
}
